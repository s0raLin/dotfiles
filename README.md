# Linux 配置管理器

一个现代化的 Linux 系统配置文件管理工具，帮助你整合和管理常用的配置目录和文件。

## 功能特性

- 🔧 统一管理常用配置文件 (bashrc, zshrc, vimrc, gitconfig 等)
- 📁 配置文件分类和组织
- ✏️ 内置编辑器支持语法高亮
- 🔄 配置文件备份和恢复
- 🔍 快速搜索和定位配置项
- 📊 配置文件状态监控
- 🎨 现代化的用户界面

## 支持的配置类型

- **Shell 配置**: .bashrc, .zshrc, .profile, .bash_profile
- **编辑器配置**: .vimrc, .nvim/, .emacs
- **Git 配置**: .gitconfig, .gitignore_global
- **SSH 配置**: ~/.ssh/config, known_hosts
- **系统配置**: /etc/hosts, crontab
- **应用配置**: 各种应用的配置文件

## 本地运行

**前置要求:** Node.js 18+ 和 pnpm

1. 安装 pnpm (如果还没有安装):
   ```bash
   npm install -g pnpm
   ```

2. 安装依赖:
   ```bash
   pnpm install
   ```

3. 运行开发服务器:
   ```bash
   pnpm dev
   ```

4. 在浏览器中打开 http://localhost:5173

## 构建

```bash
pnpm build
```

## 预览构建结果

```bash
pnpm preview
```
